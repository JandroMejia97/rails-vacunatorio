<div class="row mb-4">
  <div class="col-12 d-flex justify-content-between align-items-center">
    <h1>
      <%= t 'vaccination_center.title' %>
    </h1>
  </div>
</div>


<div class="row">
  <% @vaccination_center.each do |vaccination_center| %>
    <div class="col-lg-6 col-md-6 col-sm-12 col-xs-12 mb-3">
      <div class="card">
        <div class="card-header">
          <h2>
            <%= vaccination_center.name %>
          </h2>
        </div>
        <div class="card-body text-center <%= 'empty' unless vaccination_center.length > 0 %>">
          <% stock = get_quantity_of_vaccines_available campaign.vaccines %>
            <% if vaccination_center.length > 0 %>
              <table class="table table-striped">
                <thead>
                  <tr>
                    <th scope="col">
                      <%= t 'base_text.name' %>
                    </th>
                    <th scope="col">
                      <%= t 'vaccination_center.address' %>
                    </th>
                    <th scope="col">
                      <%= t 'base_text.actions' %>
                    </th>
                  </tr>
                </thead>
                <tbody>
                    <tr>
                      <th scope="row">
                        <%= vaccination_center.name %>
                      </th>
                      <td>
                        <%= vaccination_center.address %>
                      </td>
                      <td>
                        <%= link_to t('base_text.show'), vaccine, class: "btn btn-outline-secondary btn-sm" %>
                        <%= link_to t('base_text.edit'), edit_vaccine_path(vaccine), class: "btn btn-outline-primary btn-sm" %>
                        <%= link_to t('base_text.destroy'), vaccine, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-outline-danger btn-sm" %>
                      </td>
                    </tr>
 
                    <tr>
                      <% if stock > 0 %>
                        <th scope="row" colspan="2">
                          <%= t 'vaccines.vaccines_available' %>
                        </th>
                        <td>
                          <%= stock %>
                        </td>
                        <td>
                          <%= link_to t('vaccines.assign_shifts'), "", class: "btn btn-secondary btn-sm disabled" %>
                          <%# <%= link_to t('vaccines.load_batch'), "", class: "btn btn-primary btn-sm disabled" %>
                        </td>
                    <% else %>
                      <td colspan="4">
                        <%= t 'vaccines.no_vaccines_available' %>
                      </td>
                    <% end %>
                    </tr>
                </tbody>
              </table>

              <p>
                <%= t 'vaccines.empty' %>
              </p>
            <% end %>
          <% end %>
          <%# <%= link_to t('vaccines.add_vaccine'), { action: "new", controller: "vaccines", campaign: campaign.id}, class: "btn btn-primary mt-3" %>
        </div>
      </div>
    </div>
  <% end %>
</div>
